#include <bits/stdc++.h>

using namespace std;

int b,k, sz;
string digits;

int getRes(int mask) {

    if(mask == (1<<sz) - 1) {

    }
    if()
}

int main() {

    int T;
    cin >> T;
    for(int t=1; t<=T; t++) {

        //int b,k;
        cin >> b >> k;
        string digits;
        sz = digits.size();
        //cin >> digits;

        getRes(0);
    }

}
