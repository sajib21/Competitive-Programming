#include <stdio.h>
#include <bits/stdc++.h>

using namespace std;

int main()
{
    char c, s[1010];
    scanf("%c", &c);
    printf("%c", toupper(c));
    gets(s);
    printf("%s\n", s);

    return 0;
}
