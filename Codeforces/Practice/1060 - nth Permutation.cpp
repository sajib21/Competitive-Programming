#include<bits/stdc++.h>

using namespace std;

int n, sz, alphaCount[100];
string seq;

int dp[]

int getNth(int indx) {



}




int main() {

    int T;
    scanf("%d", &T);
    for(int t=1; t<=T; t++) {

        memset(alphaCount, 0, sizeof(alphaCount));
        memset(dp, -1, sizeof(dp));

        cin >> seq;
        scanf("%d", &n);

        sz = seq.size();
        for(int i=0; i<sz; i++) alpha[ seq[i] ]++;

        printf("Case %d: %")

    }
}
