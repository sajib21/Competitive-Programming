#include<bits/stdc++.h>

using namespace std;

int main() {

    int t;
    int graph[1000][1000];
    cin >> t;

    while(t--) {

        memset(graph, 0, sizeof(graph));

        int i=1;
        char c;
        while(scanf("%c", &c) && c != '\n') {

        }
    }
}
