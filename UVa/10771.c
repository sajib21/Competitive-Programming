#include <stdio.h>
int main()
{
    int n, m, k;

    while(scanf("%d %d %d", &n, &m, &k)==3)
    {
        while(1)
        {
        if(k=0)
        {
            printf("Gared\n");
            break;
        }

        }
    }
}
